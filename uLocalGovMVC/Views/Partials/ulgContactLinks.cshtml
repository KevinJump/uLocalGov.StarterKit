@using System.Linq 
@inherits Umbraco.Web.Mvc.UmbracoTemplatePage

@*
 displays the contact links on a page
*@

@{
    if ( Model.Content.HasProperty("Contact") && Model.Content.HasValue("Contact") ) 
    {
        var Contact = Umbraco.TypedContent(Model.Content.GetPropertyValue("Contact"));
        
        if ( Contact != null )
        {
         <div class="contact-details well">
             <h2>@umbraco.library.GetDictionaryItem("Contact Us")</h2>
             <ul class="nav nav-list">
                 
                 @if ( Contact.HasValue("emailAddress") ) 
                 {
                    <li><a href="@Contact.Url#email">@umbraco.library.GetDictionaryItem("eMail")</a></li>
                 }
                 
                 @if ( Contact.HasValue("telephone") )
                 {
                     <li><a href="@Contact.Url#phone">@umbraco.library.GetDictionaryItem("Telephone")</a></li>
                 }

                 @if ( Contact.HasValue("address") ) 
                 {
                     <li><a href="@Contact.Url#address">@umbraco.library.GetDictionaryItem("Address")</a></li>
                 }
                 
             </ul>
         </div>   
        }
    }
}
